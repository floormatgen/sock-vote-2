# sock-vote-2

The sequel to sock-vote.

## Server API

To see the current version of the server API, copy the contents of `swagger.yml` into an editor such as [**SwaggerEditor**](https://editor-next.swagger.io/).